name: wakeup_in_time
description: A wake-up alarm that requires a push-up verification.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.1
  flutter_local_notifications: ^18.0.1
  flutter_timezone: ^5.1.0
  google_mlkit_pose_detection: ^0.15.0
  timezone: ^0.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
